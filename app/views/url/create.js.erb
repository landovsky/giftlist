$('#url').val('');
url = $("#<%= j (@url.digest)%>")
if( url.length === 0 ) {
$('#result').append("<%= j (render 'result') %>");
} else {
	url.animate({ opacity: 0.25 }, 300, function() {
    url.animate( { opacity: 1}, 300 ); }
    );
}