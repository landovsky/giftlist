<h3><%= @list.occasion%></h3>
<h4><%= @list.occasion_of%></h4>

<%= render "shared/alert-box" %>
<%= render "shared/alert-box-js" %>

<p>
	<%= link_to 'zpět', lists_path, class: "btn btn-default" %>&nbsp;
	<%= link_to @list.invitations_button_label, '#', id: "invitation_btn", class: "btn btn-success" %>
</p>

<%= render 'users/invitations' %>
<%= render partial: 'gifts/gift_form', gift: @gift %>
<%= render partial: 'gift', collection: @gifts, as: :gift %>
<%= render partial: 'gift_empty' if @gifts.blank? %>


<script>
	i = $("#invitations") 
	$("#invitation_btn").on("click", function () {
		if ( i.is(":visible") ) {
			i.hide(200);
			event.preventDefault();
		} else {
			i.show(200);
			event.preventDefault();
		}
	});
</script>
